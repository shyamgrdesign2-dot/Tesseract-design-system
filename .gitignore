# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
/dist
dist-ssr
# (mcp/dist/server.mjs — the bundled MCP server — IS tracked; it ships in the plugin)
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static
.gstack/

# Icons are served 100% from the CDN — never store the icon library locally.
# This guard keeps any local dev copy out of git.
public/tp-icons/

# Secrets / local config — never commit
.mcp.json
.env
.env.*
*.local
.npmrc
