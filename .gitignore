# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static
.gstack/

# Vendored TP icon library (~102MB, 25k files). Distributed separately and
# copied into public/ per the adoption guide — kept out of git history.
# EXCEPT the curated dr-agent set, which lives under the same root but is
# tracked. (Medical icons are now merged into the library's linear/bulk/bold.)
public/tp-icons/*
!public/tp-icons/dr-agent/

# Secrets / local config — never commit
.mcp.json
.env
.env.*
*.local
.npmrc

# Stray scratch file
Untitled
